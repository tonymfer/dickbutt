'use client';

import styled from 'styled-components';
import { WIN98_COLORS } from './constants';

export const Win98Toolbar = styled.div`
  display: flex;
  align-items: center;
  padding: 2px;
  background: ${WIN98_COLORS.face};
`;
